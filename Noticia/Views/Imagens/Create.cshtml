@model Noticia.Models.Imagens

@{
    ViewData["Title"] = "Criar Imagem";
}

<h1>Criar</h1>

<h4>Imagens</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label asp-for="Nome" class="control-label"></label>
                <input type="file" name="foto" accept=".jpg,.png" style="color:blue;" />

                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Legenda" class="control-label"></label>
                <input asp-for="Legenda" class="form-control" />
                <span asp-validation-for="Legenda" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Adicionar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
